import "./App.css";
import Board from "./components/Board";

function App() {
  return (
    // BOARD
    // SQUARE
    // O's and X's (We will be using images for this)
    <div>
      <Board />
    </div>
  );
}

export default App;
